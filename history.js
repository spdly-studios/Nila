const esc = v => String(v || '—').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function loadCalls() { const response = await fetch('/api/calls'); const calls = response.ok ? await response.json() : []; document.querySelector('#calls').innerHTML = calls.length ? calls.map(call => { const vars = (call.request || {}).variables || {}; return `<tr><td>${esc(vars.name || call.student)}</td><td>${esc(vars.pname || call.parent)}</td><td>${esc(call.status)}</td><td>${esc(call.callSummary || call.dispositionResult || call.webhook?.data?.callSummary)}</td><td>${esc(call.updatedAt || call.createdAt)}</td></tr>`; }).join('') : '<tr><td colspan="5">No calls have been submitted yet.</td></tr>'; }
loadCalls().catch(() => { document.querySelector('#calls').innerHTML = '<tr><td colspan="5">Unable to load call history.</td></tr>'; });
