document.write('<script src="js/navigation.js"><\\/script>'); const keys = { endpoint: 'attendly-ai-endpoint', model: 'attendly-ai-model', tagModel: 'attendly-ai-tag-model' }; for (const [id, key] of Object.entries(keys)) document.querySelector('#' + ({ endpoint: 'aiEndpoint', model: 'aiModel', tagModel: 'aiTagModel' }[id])).value = sessionStorage.getItem(key) || ''; saveSettings.onclick = () => { for (const [id, key] of Object.entries(keys)) sessionStorage.setItem(key, document.querySelector('#' + ({ endpoint: 'aiEndpoint', model: 'aiModel', tagModel: 'aiTagModel' }[id])).value.trim()); settingsStatus.textContent = 'Saved for this browser session.' }; clearSettings.onclick = () => { Object.values(keys).forEach(k => sessionStorage.removeItem(k)); location.reload() }; window.addEventListener('error', e => console.error('[Attendly]', e.error || e.message)); window.addEventListener('unhandledrejection', e => console.error('[Attendly]', e.reason));
const staff = document.querySelector('.sidebar-foot strong'); if (staff) staff.textContent = 'Shivaprasad V';
